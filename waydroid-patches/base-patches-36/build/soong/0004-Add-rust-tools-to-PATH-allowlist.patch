--- a/ui/build/paths/config.go
+++ b/ui/build/paths/config.go
@@ -87,6 +87,8 @@ func GetConfig(name string) PathConfig {
 // run during the build. For more documentation, see path_interposer.go .
 var Configuration = map[string]PathConfig{
 	"bash":        Allowed,
+	"bindgen":     Allowed,
+	"cbindgen":    Allowed,
 	"date":        Allowed,
 	"diff":        Allowed,
 	"dlv":         Allowed,
@@ -106,6 +108,8 @@ var Configuration = map[string]PathConfi
 	"perl":        Log,
 	"pstree":      Allowed,
 	"rsync":       Allowed,
+	"rustc":       Allowed,
+	"rustfmt":     Allowed,
 	"sh":          Allowed,
 	"stubby":      Allowed,
 	"tar":         Allowed,
